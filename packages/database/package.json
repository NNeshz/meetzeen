{
  "name": "@meetzeen/database",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "DATABASE_URL=postgresql://dummy:dummy@localhost:5432/dummy DIRECT_URL=postgresql://dummy:dummy@localhost:5432/dummy bunx prisma generate --schema=prisma/schema/schema.prisma",
    "prisma-command": "bun --env-file=../../.env --stdio prisma"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "5.5.4"
  },
  "prisma": {
    "schema": "prisma/schema"
  }
}